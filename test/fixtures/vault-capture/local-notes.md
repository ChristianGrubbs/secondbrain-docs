# Wibbleflux Local Notes

This local Markdown fixture proves that sb-docs capture preserves the exact
sentinel phrase WIBBLEFLUX-3390, a fenced code block, a Unicode heading
character (café, naïve, 日本語), and a relative link to a local image asset.

```bash
echo "wibbleflux"
```

See the reference diagram: ![pixel](./pixel.png)
