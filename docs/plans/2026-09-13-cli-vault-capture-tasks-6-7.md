# CLI vault capture: remaining Tasks 6–7

Updated 2026-09-13. Status: planned; neither task is accepted by this document.

## Authority and baseline

This is the execution plan for the remaining migration work. It supersedes Tasks 6–7 and the continuation instructions in the [2026-09-09 migration plan](/Volumes/3M/markitdown-secondbrain-vault-optimized/docs/superpowers/plans/2026-09-09-cli-vault-capture-migration.md). That document retains the product contract and Tasks 1–5 history; the shipped decisions below take precedence over its proposed implementation details. The operator requested this refresh after Task 5 on 2026-09-13. This update authorizes no claim of qualification or installed-client readiness.

Execution repository: `/Volumes/3M/github-repos/secondbrain-docs`. Canonical project record: [project MOC](/Volumes/3M/Obsidian/40%20Projects/MarkItDown%20Second%20Brain/index.md). Planning evidence was checked on 2026-09-13 against fork HEAD `75501fa434968d5f27a7adecdb2eebfbcf93533c`; re-resolve the implementation base and active owners before execution. Reconciliation on that date left both remaining tasks status-unverified because the old task entries lacked scoped evidence mappings. Do not tick either task from this plan refresh or from the Task 5 test total.

| Shipped slice | Evidence | Binding lesson for the remaining work |
| --- | --- | --- |
| Task 1: restricted executable | [PR #1](https://github.com/ChristianGrubbs/secondbrain-docs/pull/1), `89ce650` | Exercise the built executable directly, including shebang, exit and no-listener behavior. Provision the lockfile-matched Chromium explicitly. |
| Task 2: source/MOC publication and corrections | [PR #2](https://github.com/ChristianGrubbs/secondbrain-docs/pull/2), `72a6407`; [PR #3](https://github.com/ChristianGrubbs/secondbrain-docs/pull/3), `f662fc3` | Real CLI stderr is part of the adapter contract. Preserve UTF-8 across stream chunks, classify CAS separately, deduplicate exact links, and reuse established collection-folder spelling. Filenames use a 12-character identity prefix with full-hash collision fallback. |
| Task 3: recoverable updates | [PR #4](https://github.com/ChristianGrubbs/secondbrain-docs/pull/4), `08d5c56` | Whole-note ownership and CAS protect hand edits. Layout 2 publication state is durable. Per-source SQLite `BEGIN EXCLUSIVE` locks are kernel-released on process death; discovery rereads current notes inside the lock. Do not replace this with pid/heartbeat reclamation or a discovery cache. |
| Task 4: capture | [PR #5](https://github.com/ChristianGrubbs/secondbrain-docs/pull/5), `183ac8a` | Preserve keyed page outcomes, one `run_error`, and exits 0/2/1/130. Publication errors never become acquisition failures; cancellation preserves prior useful outcomes. |
| Task 5: retrieval from saved notes | [PR #7](https://github.com/ChristianGrubbs/secondbrain-docs/pull/7), `75501fa` | Per-collection generations and pointers, shared state-level SQLite lock, structural generation classification and encoding fingerprint; saved bytes and the shared frontmatter parser drive indexing. Reader and writer recovery must preserve every sibling note. Recorded final suite: 147 files / 2280 tests. |

Capture capability returned with Task 5. Installed entry points and the complete format matrix remain unqualified. The old runtime was deleted on 2026-09-10; there is no old-engine rollback. Preserve all existing vault notes, attachments and links. Historical receipts/runtime databases were deleted, so no remaining step may depend on retaining them on disk.

## Invariants carried into both tasks

**Partially present derived state must never read as an empty answer.** Only a genuinely never-built collection has neither a pointer nor any generations. Absence, invalidity and corruption are distinct from that state. A damaged collection must recover from the vault or visibly fail the retrieval operation; it must not return `status: ok` with an empty result as if the query honestly missed. Apply this on both search and capture/upsert entry paths.

Capture success and retrieval readiness remain separate. Exit 0 means useful publication is complete, including its MOC link; `index: pending` may accompany it. Exit 2 means useful but incomplete publication/capture, 1 means no useful publication, and 130 means cancellation. Clients inspect the full JSON envelope and index status, preserve stderr diagnostics, and never translate an operation failure or omitted/stale result into an honest miss. Search may report omissions with exit 0; do not promise a new exit contract without an explicit tested change.

All vault access uses `obsidian-cli`. Prove `OBSIDIAN_VAULT` points at the throwaway vault before a mutating fixture, then assert the live vault is untouched. Test actual CLI processes as well as fakes: exit 1 alone cannot distinguish missing paths, unavailable backends and heading errors. Index repair deletes/rebuilds only derived generations; durable journals and ownership records survive. No original source fetch, embedding call, telemetry call or listener is permitted for retrieval/reindex.

Use Node 22 for new qualification/install work per the repository contract. Previous runs recorded Node `v26.8.2`; those results are historical evidence, not Node 22 qualification. Record actual Node/ABI, architecture, native SQLite loads and Chromium version before running the matrix. Any intended toolchain-policy change must be resolved explicitly rather than inferred from a successful native import.

## Task 6: qualify capture, recovery and agent retrieval

**Owner:** integration worker (Sol/medium or native Sonnet/medium), with one opposite-family reviewer. Escalate generation classification, locking or publication ownership fixes to the frontier class with a recorded reason. Resume the original slice implementer for those fixes with frontier model/effort where supported; replace it only if unavailable, transferring its exact evidence and ownership. The reviewer remains read-only and never becomes the fix author. Task 6 completes only with a row-by-row evidence report for the exact release candidate; client changes stay gated on that acceptance.

Resolve that implementer from the affected slice's MOC log and linked review/handoff record, then verify the session is addressable through coordinator discovery. The baseline table above maps each slice to its PR. For example, Task 5's MOC log records the Opus worker branch `worktree-agent-acac340221232f478`. A branch or model label alone is not a resumable session ID: if the records and coordinator cannot resolve one, record it as unavailable and assign one replacement with the exact base, findings and exclusive file scope. Record every new worker/session in the task evidence so later fixes have a direct owner reference.

**Files:** create `docs/migration-qualification.md`, `scripts/live-check-vault.mjs`, `test/vault-capture-e2e.test.ts`, and required fixtures under `test/fixtures/vault-capture/`. Reuse real files under `test/fixtures/`; extend existing `test/vault-index-e2e.test.ts`, `test/vault-publish-e2e.test.ts`, `test/vault-cli-e2e.test.ts`, `src/vault/VaultIndex.test.ts` and `src/vault/ObsidianCli.test.ts` only where assertions are missing. Keep unit/integration cases in each module's existing test file. A discovered defect gets its own scoped fix and regression evidence rather than an unbounded acquisition rewrite.

### 6A — freeze inputs and isolate the environment

- [ ] Inventory the actual fixture files and inspect known facts before freezing the rows below. Each row records stable ID, exact source/fixture and hash or revision, expected pages and vault paths, collection/version, factual strings, code/table/link/Unicode assertions, required local assets, agent query and expected note identity, plus pass/fail/blocked evidence. List planned rows as unrun until executed; a skipped live row is not a pass.
- [ ] Freeze these required rows: single docs page; GitHub README; GitHub blob; bounded two-page site; JS-rendered page; local Markdown; text PDF with a table; DOCX; mixed-file directory; same source at two versions; manually edited existing capture; acquisition/conversion failure; PPTX; XLSX; ipynb; JSON; XML; plain text; source code; ZIP. Required document-local assets must be copied and linked before that row qualifies; remote-image references alone do not prove local asset preservation.
- [ ] Give every recovery case in 6C and every residual item in 6D its own evidence ID in the same report. The FTS divergence row records the accepted detection limit, its source and whether an optional unsupported-corruption probe was run; do not invent a passing detection test. The pruning-failure and sandboxed config-write probes are required.
- [ ] Record the fork and upstream SHAs, toolchain probe, CLI version and exact isolated vault/state/config locations. Use a controlled explicit `DOCS_MCP_CONFIG` for normal fixture runs so qualification cannot rewrite the operator's system config. Run the separate unset-config probe only in a sandbox home/config environment. Prove the vault redirect before any capture. Never delete the whole state directory as an index-reset shortcut.

### 6B — exercise the real publication boundary

- [ ] Spawn the built executable directly for each required row. Assert useful factual content in saved Markdown, exactly one source link, metadata, whole-note digest, query result identity and full `read` bytes. An exit 0 or one generic search hit cannot qualify a row. Live HTTP/GitHub/JS rows have dated source evidence alongside deterministic local fixtures; unavailable live dependencies remain blocked.
- [ ] Start with no inbox folder and separately with no Doc Sets parent or named collection. First capture into a new non-inbox collection must work against the installed real `obsidian-cli`, exercising both `not a directory` and `no such directory for:` missing-directory diagnostics in adapter tests. Backend/permission errors must still fail visibly. Repeat with spaces, Unicode and an established mixed-case collection folder; do not create a second case variant.
- [ ] Reuse Task 2–4 tests for split UTF-8 output, EPIPE, exact MOC link matching, unchanged byte preservation, body/frontmatter edits, frozen paths after title change, ownership-state loss, immutable conflict reuse, CAS races, interrupted publication and kernel lock release. Add process coverage only where it is absent. For partial, all-failed and cancelled captures, assert 2/1/130 and preserved prior notes; publication exceptions must not increment upstream acquisition-failure counters.
- [ ] Record capture subprocess counts, vault list/read counts and elapsed time for the two-page and mixed-directory rows. The accepted per-capture full-collection scan is a measured cost; do not invent a threshold or install a cache to make the report look faster. Inventory legacy notes/links read-only; optional explicit indexing uses their saved bytes without adoption, rewriting or recapture.

### 6C — prove damaged state cannot become a false miss

- [ ] Reuse the existing nine-shape `VaultIndex.test.ts` table as the minimum contract: impossible pointer target, malformed pointer JSON, deleted pointer with generations remaining, deleted generation directory, old manifest format, non-object manifest row, previous store encoding, missing database and unreadable database. Execute each through **search and upsert**. Seed multiple sibling notes and a second collection; assert complete expected membership and sibling digests after repair, never only the newly captured note. Add process-level representatives for malformed/deleted pointers, malformed manifest rows and deleted databases so CLI/result handling is also covered.
- [ ] Distinguish a genuinely never-built empty collection and a healthy honest query miss from every damaged-state case. When recovery is blocked by backend unavailability, held lock or failed verification, search/reindex fail visibly and clients retain that failure. A successful repair returns the expected saved-note facts. Test full index loss with saved notes still present, using a separately recorded never-built control with no notes.
- [ ] Seed collections A and B with multiple notes; rebuild A, then B, then reconstruct both after deleting only derived index data. Inspect promoted manifests/databases and expected membership **before any search can lazily repair an incomplete rebuild**. Remove the original local sources or deny their fetches, then assert both collections answer from current vault bytes. Preserve manual edits, exclude conflict candidates, and keep case/whitespace-distinct version identities distinct after upstream normalization.
- [ ] Reuse count/encoding verification tests and test a failure after generation construction starts: no bad promotion and prior usable generation unchanged. Under a held index lock capture may publish with `index: pending` and exit 0, while search/reindex fail their own operation. Release the lock, rebuild from saved notes and verify readiness. Include a note edited, renamed or deleted while its update waits; status must describe the actual indexing outcome.
- [ ] Preserve frontmatter exclusion, saved-byte digests and no-fetch/no-embedding/no-listener assertions. Probe tokens come from indexed splitter output. Ranked search limits apply to chunks before page grouping, so top-N search is not proof of index completeness; use manifest/database membership plus per-note query probes.

### 6D — record residual issues and acceptance

| Open item from Task 5 | Required disposition |
| --- | --- |
| Post-promotion FTS/document divergence at unchanged document row count is not detected | Keep this as an explicit coverage limit. Existing module writes maintain both through upstream transactions/triggers; do not claim exhaustive corruption detection. A full-table check per search was deliberately deferred. Reopen only if qualification finds a reachable module-write path or new integrity requirements; a deliberate unsupported corruption probe records the limitation, never a qualification pass for detection. |
| Generation pruning retention is best-effort | Exercise removal failure without losing the active generation or turning valid results into failure; record/log leftovers and a later rebuild retry. Do not require an exact retained-generation count or delete durable publication state to clean up. |
| `loadConfig()` auto-writes the system config when `DOCS_MCP_CONFIG` is unset | **Resolved 2026-09-14** (read-only loading shipped for every vault command; see `docs/plans/2026-09-14-task6-f06-f07-d03-fixes.md`). Original disposition: Track a separate scoped config-loading fix. In sandboxed process tests prove whether the file is created/changed. Before Task 7 cutover, either ship and verify read-only loading for vault commands or enforce a controlled explicit config on **every** installed entry point. The workaround must protect direct CLI, Raycast, watcher and agent calls and preserve user config bytes. Merely setting it in fixture tests is insufficient. Keep the underlying issue open if only contained. |

- [ ] Publish `docs/migration-qualification.md` with per-row results, stdout envelopes, exit codes, stderr, saved-note paths/digests, index membership, timings and residual limits. Reference existing regression evidence instead of duplicating whole suites. A required format or supported recovery failure blocks acceptance until a reviewed fix passes; an external dependency failure remains status-unverified. A one-shot converter adapter requires named scope and evidence, never the old engine.
- [ ] Preserve the Task 6 qualified source SHA and reproducible build/dependency manifest as release candidate R1. Task 7 packages R1 unchanged in an isolated install as its first rollback target, then creates and independently qualifies R2 from the actual installer/client integration changes. This supplies two real qualified releases within the existing tasks; R2 is not a relabeling of R1. Any fix needed to package R1 requires requalification and a newly recorded R1 baseline before proceeding.
- [ ] Run the relevant tests, build, typecheck/lint and required complete suite on the exact candidate. Record author/reviewer selection and runtime identity where reported. Log evidence in the project MOC, commit, push and merge the qualifying changes; tick Task 6 only with its qualification report and accepted release SHA. Task 7 consumes that SHA and report, rerunning affected rows after later changes.

## Task 7: install and switch every approved capture surface

**Dependency:** Task 6 accepted and config-write behavior fixed or mechanically contained. **Owner:** integration worker with one integrated opposite-family reviewer. Installer/pointer compatibility and watcher recovery get frontier review when they introduce concurrency or state migration. Success means a qualified installed release is usable through the direct CLI, Raycast, both agent catalogs, a Meta Bind URL widget and a Finder drop folder.

The widget and drop folder are required by the operator's 2026-09-10 decision recorded on the MOC. Use the validated Meta Bind request-inbox pattern and one small launchd watcher invoking finite `sb-docs capture` processes. No new web console or resident capture server. A JS Engine in-note drop zone was a rejected alternative, not an unresolved dependency.

**Files:** fork `scripts/install-vault-cli.sh`, `scripts/rollback-vault-cli.sh`, `scripts/live-check-vault.mjs`, `docs/cutover.md`; planned watcher/LaunchAgent source and tests under `scripts/` (exact filenames resolved in the Task 7 implementation inventory). Resolve the widget/registry vault paths, watched Finder folder, launchd label and installed-release paths before implementation; none is invented here. Existing launcher: `/Volumes/3M/Documents/Raycast-Scripts/MarkItDown/Second-Brain-Console.sh`; locate its actual source repository before editing. Resolve the canonical `sb-capture` and `audienceview` sources through committed `skill-scope` state.

### 7A — inventory and package the qualified release

- [ ] Coordinate live owners for each checkout and target note. Read reviewer routing and inspect committed skill scope; use its sanctioned mutation commands for catalog/skill changes. Resolve caller paths afresh. The 2026-09-10 stack [PR #614](https://github.com/ChristianGrubbs/ai-stack/pull/614) changed `sb-capture` to an OUT OF SERVICE tripwire, switched AudienceView to manual intake and appended the ADR 0063 retirement note. Verify installed state; do not assume those callers still invoke `sb`. The parity assertion excluding `sb capture` is a negative guard, not a missing-binary check to remove.
- [ ] On this Mac inspect `/Users/christian/ai-stack`'s checked-out SHA, committed skill-scope registration and the resolved targets/content of both agents' `sb-capture` catalog entries. Read the currently resolved AudienceView intake, ADR 0063 amendment and parity test; compare their exact content with the recorded PR #614 change. Record paths, SHAs and checked date. A merged PR or a registration name alone is not evidence that either agent currently sees those instructions.
- [ ] Package the accepted SHA with lockfile-pinned dependencies, explicit Node 22 and matching Chromium; verify native dependencies on the installed runtime. Use immutable per-release directories and an atomic current pointer. Keep layout-2 publication journals/ownership outside releases; preserve their location and backups across installation/rollback. Record executable, runtime and config resolution in the release manifest; launcher/launchd execution must not depend on an interactive shell PATH.
- [ ] Test installation in sandbox home/config locations. Confirm it protects existing user config, works with a fresh shell and rejects invalid state paths including symlink aliases into the vault. If using explicit-config containment, enforce it in the installed executable wrapper so direct calls cannot bypass it by omitting `DOCS_MCP_CONFIG`. Record config behavior for capture/search/reindex and confirm other commands do not cause the inherited write.

### 7B — connect the approved clients without hiding partial results

- [ ] Replace Raycast console actions with capture accepting URL/file input, argument-safe invocation and a result pointing to the saved note. Exercise a real URL and a local path with spaces/Unicode. Show publication result and indexing readiness separately; preserve partial failure, conflict and cancellation information.
- [ ] Build the in-vault Meta Bind URL input/request widget plus watched Finder folder and one launchd watcher. Resolve queue schema and exact paths from the prior-art record before editing. Accept pasted URLs and fully written files; persist request identity, processing/result state and retry information outside debug logs. Serialize/claim requests so two watcher starts cannot dispatch one request concurrently. On interruption inspect durable request/publication outcomes; do not claim exactly-once processing from a pid file. Preserve dropped originals and failed requests, handle partial file copies, and let users find saved notes, conflicts and failures. All vault queue/widget/result changes use `obsidian-cli` and CAS where applicable.
- [ ] Test URL paste, file drop, duplicate observation, file still being copied, watcher restart during capture, CLI unavailable, exit 2, exit 130, and index pending/unavailable. Assert no silent request loss or duplicate MOC links, retained input, visible terminal outcome and retry/recovery behavior. Do not change the trusted publication/index locks to solve client queue coordination. Record the chosen bounded retry policy in `docs/cutover.md`.
- [ ] Restore the capture skill using verified command help and fixtures: capture/search/read/reindex/doctor, default folders, collection/version identity, JSON outcomes, explicit conflict handling and full-note retrieval. Search results are excerpts; `read` supplies the complete saved source. Agents must distinguish honest miss, reported omissions and unavailable retrieval and must not auto-adopt notes or erase journals. Repoint current AudienceView intake and append the current ADR guidance; retain historical decisions. Scan live callers, distinguish active instructions from frozen history, and verify both agent catalogs and the parity tests.

### 7C — switch, prove live behavior, and rehearse recovery

- [ ] Document the exact qualified release, config, durable state, watcher/queue schema and client-file compatibility before changing the active pointer. Quiesce the watcher and in-flight captures at a stable boundary for switch/rollback. Snapshot only the client/config artifacts necessary to restore the previous release; never restore old journal bytes over newer publications. Use skill-scope and vault CAS for those surfaces rather than raw file-copy rollback.
- [ ] Produce the two-release prerequisite explicitly: install the preserved Task 6 R1 into an isolated release directory and verify its manifest/CLI checks; build R2 from Task 7's actual integration commit and run the frozen qualification suite plus all new client/install tests on R2. Record both SHAs and compatibility evidence. Rehearse R2 → R1 → R2 there before production activation; only after success install the same qualified artifacts for the final switch. R1 need not have been a historical production install to be a real tested recovery target, and its recorded baseline client configuration is restored without claiming it provided R2-only surfaces.
- [ ] During that isolated rehearsal capture under R2, switch to R1 and restore its recorded client files, then prove R1 can discover/read preserved notes and safely accept or rebuild its derived encoding without losing siblings before switching forward. If durable state/queue compatibility fails, refuse rollback and fix forward. Before R2 is qualified, record rollback as unavailable; disabling the new client and preserving input while fixing forward is the recovery path. Do not manufacture a previous release or claim a same-build pointer test establishes cross-release compatibility.
- [ ] Ship branch → PR → merge → documented install, then run live checks through the resolved installed executable and each required client: CLI capture/search/read/reindex, Raycast, both agent catalogs, Meta Bind URL request, Finder file drop and watcher restart. Record request-to-note linkage, saved bytes/digests, actual release/runtime/config and meaningful results. Run the frozen qualification suite on the final candidate; rerun affected rows after fixes. Live captures go only to explicitly designated collections and remain as source notes.
- [ ] After successful rollback rehearsal, switch forward and repeat capture plus complete-note retrieval. Capture failures, damaged-index false misses, attribution errors, lost manual bytes, wrong executable or dropped requests stop acceptance. Preserve inputs, notes and durable journals; derived index reconstruction is the recovery mechanism. If only one qualified release exists, keep the cross-release rehearsal unchecked and Task 7 incomplete rather than inventing evidence.
- [ ] Record the integrated review, installed SHA, live-surface evidence, compatibility/rehearsal evidence and all residual limitations in `docs/cutover.md` and the project MOC. Tick Task 7 only when every required surface and recovery criterion has its own evidence. Preserve the FTS and retention limitations and leave the config issue explicitly open if it is contained rather than fixed.

## Continuation and completion

Resume at Task 6A using this file; do not restart completed Tasks 1–5 or the deleted-engine backlog. The current request updates planning only. Completion of the migration requires both remaining task reports, the accepted installed release, every approved surface and complete saved-note retrieval. A shipped slice or a green test total does not prove whole-project acceptance.
